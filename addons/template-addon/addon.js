export const info = {
    "name": "Template",
    "description": "A template for creating addons for Canvas LMS",
    "version": "1.0.0",
    "settings": {}
}

export const main = {
    init: function() {
        // when the addon is initialized
    },

    start: function(addon, parameters) {
        // when the addon is started
    }
}